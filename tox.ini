[tox]
envlist = py312
skipsdist = True

[testenv]
deps =
    -r requirements-dev.txt
    pytest-cov
commands =
    pytest --cov=commuted_calligraphy --cov-report=xml --cov-config=tox.ini --cov-branch --junitxml=test-results.xml

[coverage:run]
relative_files = True
