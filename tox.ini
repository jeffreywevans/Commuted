[tox]
envlist = py312
skipsdist = True

[testenv]
deps =
    -r requirements-dev.txt
    pytest-cov
commands =
    pytest --cov=tools --cov-report=xml --cov-config=tox.ini --cov-branch --junitxml=test-results.xml

[coverage:run]
relative_files = True
