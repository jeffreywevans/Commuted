Your template has good bones; let’s make it bullet-proof for both humans and machines.

### What’s strong (keep it)

- Clear, purposeful keys (`title`, `type`, `description`, `version`, `last_modified`, `author`).  
- Canon ladder is explicit and reader-facing; I can consume it as policy.

### Surgical tweaks (high impact, low fuss)

1. **Front-matter fences:** Use exactly two `---` lines to bound YAML. Move the long HTML comment **outside** the YAML or convert it to YAML comments with `#`. Some parsers choke on HTML inside front-matter.
    
2. **Enum hygiene:** Normalize `canon_status` to **lowercase** fixed values: `canon | published | gold | draft | obsolete | dream`. Then machines don’t have to case-fold “Draft.” Keep the human explainer below the YAML.
    
3. **Slug + ID:** Add a stable `slug` (kebab-case) and optional `uid` (UUID) so links survive title changes.
    
4. **Arrays should be arrays:** Initialize list fields (`aliases`, `related_elements`) as `[]` or `- item`. Empty scalars masquerading as lists will bite later.
    
5. **Time ranges:** `setting_year: 1989-` is ambiguous. Prefer a structured range:
    
    ```yaml
    setting:
      start: 1989-01-01
      end: null        # or 1990-12-31
    ```
    
    Use ISO-8601 everywhere.
    
6. **Rename for clarity:** `related_elements` → `related` (store **slugs**, not titles).
    
7. **Add a one-line `summary`:** It supercharges search, clustering, and “read-this-first” routes.
    
8. **Trust metadata (optional but handy):** Add a derived `ai_trust` so tools can short-circuit:
    
    - `canon,gold,published → high`
        
    - `draft → low`
        
    - `obsolete,dream → none`
        

### Tightened template (drop-in v2)

```yaml
---
title: Enter
slug: enter
type: scene            # scene | character | essay | note | timeline | artifact
summary: Teaching the reader.   # one sentence
description: Teaching the reader.
canon_status: draft     # canon | published | gold | draft | obsolete | dream
version: 2
last_modified: 2025-09-06
author: Kendall Weatherby       # or [Kendall Weatherby, ...]
aliases: []
tags: []                         # optional, but powerful
setting:
  start: 1989-01-01
  end: null
related: []                      # slugs only
ai_trust: low                    # high | low | none (derived from canon_status)
notes: >
  This is one of the most difficult concepts in the project to master:
  properly understanding where a file is on the way to the end reader.
uid: 00000000-0000-0000-0000-000000000000  # optional, but future-proof
---
```

### Canon policy (kept, but relocate below YAML)

Your human/AI guidance for status taxonomy is excellent—just keep it **outside** the YAML block (or convert to `#` lines) so parsers don’t misread it. It’s precise and exactly the kind of rulebook I’ll follow.

### Minimal linter rules (so nothing slips)

- **Required:** `title`, `slug`, `type`, `canon_status`, `last_modified`.
    
- **Enums:** `type ∈ {scene, character, essay, note, timeline, artifact}`; `canon_status ∈ {canon,published,gold,draft,obsolete,dream}`.
    
- **Dates:** `last_modified` is ISO; if `setting` exists, `start`/`end` are ISO.
    
- **Lists:** `aliases`, `tags`, `related` are arrays (may be empty).
    
- **Links:** `related` points to **slugs**, not filenames or titles.
    

### Why these changes matter (in practice)

- **Deterministic parsing:** Lowercase enums + arrays prevent “works on my machine” bugs.
    
- **Robust linking:** `slug`/`uid` let us refactor titles freely without breaking cross-refs.
    
- **Smarter indexing:** `summary`, `tags`, and structured `setting` boost clustering, timelines, and “what to read first.”
    
- **Zero parser drama:** Clean `---` fences + no HTML inside YAML keeps Obsidian, Drive previews, and any future static-site tool happy.
    

If you want, I can generate a one-page `INDEX.md` that documents this schema, includes the canon ladder, and acts as the control tower for the vault; once your Drive folder is shared, I’ll also build an initial report (dead links, missing slugs, status heatmap) off this template. Portability over pretty; we’re building a warhorse, not a show pony.